# Agent Instructions

- 默认使用中文回答。
- 用户不熟悉 Go。以后在本项目修改 Go 代码时，除了说明改了什么，还要用通俗易懂的方式解释相关源代码和本次改动涉及的 Go 语法。
- 解释要循序渐进：前期可以从最基础的概念讲起，例如 package、import、函数、结构体、指针、方法、interface、错误处理、goroutine、channel、测试等。
- 解释应结合实际代码，不要泛泛讲 Go 教程。优先引用本次阅读或修改过的文件和函数。
- 如果某个基础概念已经在前面的对话里解释过，后续不用重复展开，只在必要时简单提醒。
- 每次提交或完成一组代码改动后，主动询问用户当前 Go 学习进度或是否需要继续解释更基础的部分，以便后续调整解释深度。
- 代码改动仍以项目质量优先。教学解释不能替代必要的测试、验证和风险说明。
- 除非用户明确要求“提交”“commit”“推送”“push”，否则不要自动执行 `git commit` 或 `git push`。完成改动后只报告本地变更和验证结果。
- 如果用户明确要求提交，提交信息必须使用中文，且要简洁说明本次改动。
